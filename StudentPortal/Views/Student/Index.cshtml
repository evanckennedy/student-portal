@model IEnumerable<StudentPortal.Models.Student>

<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            background-color: #f8f9fe;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }

            .table th, .table td {
                padding: 12px;
                border: 1px solid #ddd;
                text-align: left;
            }

            .table th {
                background-color: #1d439a;
                color: white;
            }

            .table tr:nth-child(even) {
                background-color: #f2f2f2;
            }

        a {
            color: #1d439a;
            text-decoration: none;
            font-weight: bold;
        }

            a:hover {
                text-decoration: underline;
            }

        .add-new {
            display: inline-block;
            margin: 20px 0;
            padding: 10px 20px;
            background-color: #1d439a;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }

            .add-new:hover {
                background-color: #14306b;
                color: white;
            }

        .icon {
            margin-right: 5px;
        }

        .edit {
            color: #367f14;
        }

            .edit:hover {
                color: #0e5d28;
            }

        .delete {
            color: #cc0000;
        }

            .delete:hover {
                color: #7e1313;
            }

            a:hover, .edit:hover, .delete:hover, .add-new:hover {
                text-decoration: none;
            }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <section class="container view">
        <h2>Enrolled Students</h2>
        <table class="table">
            <thead>
                <tr>
                    <th>Student ID</th>
                    <th>Student Name</th>
                    <th>Email</th>
                    <th>Department</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var student in Model) {
                    <tr>
                        <td>#@student.StudentID</td>
                        <td>@student.StudentName</td>
                        <td>@student.StudentEmail</td>
                        <td>@student.Department.DepartmentName</td>
                        <td>
                            <a href="@Url.Action("UpdateStudent", new { id = student.StudentID })" class="edit">
                                <i class="fas fa-edit icon"></i>Edit
                            </a> |
                            <a href="@Url.Action("DeleteStudent", new { id = student.StudentID })" class="delete">
                                <i class="fas fa-trash-alt icon"></i>Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <a href="@Url.Action("RegisterStudent")" class="add-new">
            <i class="fas fa-plus icon"></i>Register New Student
        </a>
    </section>
</body>
</html>
